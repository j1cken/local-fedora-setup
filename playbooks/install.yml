- hosts: localhost
  connection: localhost
  vars:
    - atom_version: 1.18.0
  roles:
  - basic
  - experimental
  - docker
  - nodejs
  - google-cloud-sdk
  - { role: i3, tags: ['new'] }
  - { role: trim, tags: [] }
  - { role: blk-mq, tags: [] }
  - { role: ovpn, tags: [] }

  # - { role: blk-mq, tags: ['new'] }
